// find the length of the longest substring without repeating the charcaters (sliding windows).

function longestSubstring(str) { 
    let start = 0;
    let end = 0;
    let len = 0;
    let maxSub = "";
    let substring = "";

    while (end < str.length) { 
        const ind = substring.indexOf
    }
}